import { Config } from '../index';
/**
 * Generates a slug from content, author, and additional configurations.
 * @param config - The configuration object.
 * @returns A slug string.
 */
export declare const slug: (config: Config) => string;
